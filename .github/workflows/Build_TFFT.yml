name: Build TFFT Magisk Module

on:
  workflow_dispatch:
  
   inputs:
      tag_name:
        description: 'Tag name to build (e.g., v1.0.0)'
        required: true
        type: string

jobs:
  build:
    name: Build TFFT Magisk Module
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Create TFFT Zip
        run: |
          zip -r TFFT-${{ github.event.inputs.tag_name }}.zip . -x ".git*" -x ".github*" -x "AFFT_Banner.jpg" -x "README.md"
    
      - name: Create a tag
        run: |
          git tag ${{ github.run_id }}
          git push origin ${{ github.run_id }}

      - name: Upload TFFT to Releases
        uses: softprops/action-gh-release@v1
        with:
          files: |
            TFFT-${{ github.event.inputs.tag_name }}.zip
          tag_name: ${{ github.event.inputs.tag_name }}
          release_name: TFFT
          body: |
            TFFT ${{ github.event.inputs.tag_name }}
